{
  "name": "@matchamatch/mobile-shell",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "npm run build --workspace @matchamatch/web",
    "copy:web": "node ./scripts/copy-web-build.mjs",
    "sync": "npm run build:web && npm run copy:web && npx cap sync",
    "android": "npm run sync && npx cap open android",
    "ios": "npm run sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/cli": "^7.2.0",
    "@capacitor/core": "^7.2.0",
    "@capacitor/ios": "^7.2.0"
  }
}
